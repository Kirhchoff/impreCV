impress().init();
/*
var imp=impress();
var timeout_val = 5000;
var advance_timeout = setTimeout(advance, timeout_val);

function run_timeout()
{
  clearTimeout(advance_timeout);
  advance_timeout = setTimeout(advance, timeout_val);
}

function advance()
{
  imp.next();
  run_timeout();
}

$("#impress").on("impress:stepenter", run_timeout);*/
